const API_URL = 'http://localhost:3001/apartments';
const PORTAL_ERROR_MSG = `There is no portal container in markup. Please add portal container with proper id attribute.`;
const MODAL_CONTAINER_ID = 'modal-container-id';
const DEFAULT_URL = "/apartments?filter=all&sort=all"
const LIMIT = 24;
const LIMIT_INCREASE = 8;
const PAGE_VALUE = 1;
const PAGE_INCREASE = 0;

export {
    API_URL,
    PORTAL_ERROR_MSG, 
    MODAL_CONTAINER_ID, 
    DEFAULT_URL,
    LIMIT,
    LIMIT_INCREASE,
    PAGE_VALUE,
    PAGE_INCREASE,
};
